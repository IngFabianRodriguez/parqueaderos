# RF-008 — Operar en modo offline y sincronizar al reconectar

## Metadata

- **Módulo**: ANPR
- **Prioridad**: Alta
- **Tipo**: Funcional
- **Servicios afectados**: anpr-service, parking-service, iot-gateway

---

## User Story

**Como** Sistema (operación autónoma) **quiero** continuar operando cuando se pierde la conexión al servidor central, registrando entradas y salidas localmente **para** garantizar que el parqueadero sigue funcionando sin interrupciones aunque la conectividad a internet falle.

---

## Objetivo

Cuando el enlace de red al servidor central falla, los componentes IoT (ANPR, talanquera, sensores) deben continuar operando en modo autonomía local. Cada dispositivo mantiene una base de datos local con los últimos datos necesarios (tarifas vigentes, espacios disponibles, blacklist). Cuando la conexión se restablece, el sistema sincroniza todos los registros pendientes de manera ordenada, resolviendo conflictos.

---

## Actores

- Sistema (operación autónoma)
- [Actores secundarios]

---

## Precondiciones

      - Los componentes tienen software de autonomía local instalado
      - La última sincronización fue hace menos de 24 horas (datos locales aún válidos)
      - El modo de operación de la sede permite operación offline

---

## Postcondiciones

### En caso de éxito

      - Todos los registros realizados offline se sincronizan al servidor central
      - No hay pérdida de datos ni duplicación de transacciones
      - El sistema detecta y reporta cualquier inconsistencia al reconciliar

### En caso de falla

      - Datos locales corruptos → se requiere intervención manual y se genera alerta
      - Sincronización > 48 horas → el sistema entra en modo de solo lectura y no permite nuevas transacciones hasta sincronizar

---

## Flujo Funcional

1. IoT-Gateway detecta pérdida de conexión al servidor central (heartbeat timeout)
1. IoT-Gateway activa modo offline y notifica a ANPR-service y parking-service
1. ANPR-service comienza a almacenar registros localmente (SQLite local)
1. Vehículos siguen entrando y saliendo: ANPR captura placa, busca en cache local, registra entrada/salida
1. Tarifas se calculan con datos locales (última sincronización)
1. Cuando la conexión se restablece, parking-service envía cola de sincronización
1. Servidor central recibe cada registro, detecta timestamp, y determina si es entrada o salida
1. Servidor procesa en orden cronológico, resolviendo conflictos de estados
1. Sistema genera reporte de sincronización

---

## Datos de Entrada

| Campo | Tipo | Descripción | Requerido |
|-------|------|-------------|-----------|
| cola_registros | ARRAY | Array de registros realizados offline | Sí |
| device_id | UUID | Identificador del dispositivo IoT | Sí |
| ultima_sincronizacion | TIMESTAMP | Timestamp de última sincronización exitosa | Sí |

---

## Datos de Salida

| Campo | Tipo | Descripción |
|-------|------|-------------|
| sincronizados | INTEGER | — |
| conflictos | ARRAY | — |
| timestamp_sincronizacion | TIMESTAMP | — |

---

## Casos de Borde y Manejo de Errores

| Escenario | Comportamiento Esperado |
|-----------|--------------------------|
| Entrada offline, salida online (vehículo salió mientras sistema estaba caído) | Se detecta por timestamp: la salida online se procesa primero y la entrada offline se marca como 'sincronizada_con_conflicto' |
| Registro duplicado (misma placa dos entradas offline) | Se toma la más reciente y se loguea la anomalía |
| Tarifa cambió mientras estuvo offline | Se usa la tarifa vigente al momento del registro, no la actual |

---

## Criterios de Aceptación

- El sistema puede operar hasta 48 horas en modo offline sin perder datos críticos
- La sincronización no genera duplicación de transacciones
- Todos los registros offline tienen un identificador único generado localmente que se preserva en la sincronización

---

## Dependencias

**RFs relacionados**:
- RF-005 Captura de placa entrada
- RF-006 Salida y tarifación

**Tablas de base de datos afectadas**:
- registro_entrada (cola local)
- configuracion_sede (modo offline habilitado)
- blacklist (cache local)

---

## Endpoints API Involucrados

- `POST /api/v1/sync/cola — Sincroniza cola de registros offline`

---

## Métricas de Éxito

- Tiempo de sincronización: < 30s para hasta 1000 registros
- Cero pérdida de datos en sincronización

---

## Notas

[Supuestos, decisiones de diseño, o información adicional relevante]
