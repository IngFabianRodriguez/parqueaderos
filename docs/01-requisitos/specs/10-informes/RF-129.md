# RF-129 — Programación de Reportes Periódicos con Entrega por Email

## Metadata

- **Módulo**: Informes
- **Prioridad**: Alta
- **Tipo**: Funcional
- **Servicios afectados**: reports-service, scheduler-service, notification-service

---

## User Story

**Como** administrador de sede **quiero** programar reportes periódicos (diarios, semanales o mensuales) **para** recibir automáticamente por email los reportes más importantes sin tener que solicitarlos manualmente.

---

## Objetivo

El sistema debe permitir al usuario configurar programas de generación y entrega automática de reportes. Cada programa define: reporte a generar, frecuencia (diaria, semanal, mensual), destinatarios, formato de exportación preferido y hora de envío. El sistema ejecuta los programas en el horario configurado y envía el reporte por email a todos los destinatarios.

---

## Actores

- **Actor principal**: Administrador de sede (`tenant_admin`)
- **Actor secundario**: Superadmin, Administrador de empresa B2B (`fleet_admin`)

---

## Precondiciones

- Existe al menos un reporte configurado en el sistema.
- El servicio de email está configurado y funcional.
- Los destinatarios tienen emails válidos en el sistema.

---

## Postcondiciones

### En caso de éxito

- El programa se crea y queda activo para ejecutarse según la frecuencia configurada.
- En cada ejecución, el reporte se genera y se envía por email a los destinatarios.
- Se registra en el log de entregas el resultado de cada ejecución.

### En caso de falla

- Si el servicio de email falla, el reporte se genera igual y la entrega se reintenta hasta 3 veces con backoff exponencial.
- Si la generación del reporte falla, se envía una notificación de falla al administrador y se omite el envío.
- Si no hay datos para el período, se genera el reporte vacío con mensaje informativo.

---

## Flujo Funcional

1. El usuario accede a la sección "Reportes Programados" dentro del módulo de informes.
2. El usuario crea un nuevo programa:
   a. Selecciona el reporte base (de la lista de reportes disponibles).
   b. Define los parámetros del reporte (filtros fijos como sede, tipos de datos).
   c. Selecciona la frecuencia: DIARIA (todos los días a hora específica), SEMANAL (día de semana específico), MENSUAL (día del mes específico).
   d. Define la hora de envío (formato 24h).
   e. Agrega destinatarios (emails válidos).
   f. Selecciona el formato de exportación preferido.
3. El sistema valida los datos y crea el programa en la base de datos con estado ACTIVO.
4. El scheduler ejecuta los programas según su configuración:
   a. En la hora programada, el sistema genera el reporte con los parámetros configurados.
   b. El sistema genera la exportación en el formato elegido.
   c. El sistema envía el email con el archivo adjunto a todos los destinatarios.
   d. El sistema registra el resultado en el log de entregas (RF-130).
5. Si hay errores, se reintenta hasta 3 veces y se notifica al creador del programa.

---

## Datos de Entrada

| Campo | Tipo | Descripción | Requerido |
|-------|------|-------------|-----------|
| nombre_programa | String | Nombre descriptivo del programa | Sí |
| reporte_id | UUID | Identificador del reporte base | Sí |
| parametros | JSON | Parámetros fijos del reporte (filtros, etc.) | Sí |
| frecuencia | Enum | DIARIA, SEMANAL, MENSUAL | Sí |
| hora_envio | Time | Hora del día para el envío (formato HH:mm) | Sí |
| dia_semana | Integer | Día de la semana 1-7 (para frecuencia SEMANAL) | No |
| dia_mes | Integer | Día del mes 1-28 (para frecuencia MENSUAL) | No |
| destinatarios | Array | Lista de emails válidos | Sí (mínimo 1) |
| formato | Enum | XLSX, CSV, PDF | Sí (default: PDF) |
| zona_horaria | String | Zona horaria para la ejecución | No (default: UTC) |
| activo | Boolean | Si el programa está activo | No (default: true) |

---

## Datos de Salida

| Campo | Tipo | Descripción |
|-------|------|-------------|
| programa_id | UUID | Identificador del programa creado |
| nombre | String | Nombre del programa |
| reporte_nombre | String | Nombre del reporte base |
| frecuencia | Enum | DIARIA, SEMANAL, MENSUAL |
| siguiente_ejecucion | DateTime | Timestamp de la próxima ejecución |
| estado | Enum | ACTIVO, PAUSADO, ERROR, CANCELADO |
| destinatarios_count | Integer | Cantidad de destinatarios configurados |
| ultimo_envio | DateTime | Timestamp del último envío exitoso |
| creado_por | UUID | ID del usuario que creó el programa |

---

## Casos de Borde y Manejo de Errores

| Escenario | Comportamiento Esperado |
|-----------|--------------------------|
| Email inválido en destinatarios | Se valida al momento de creación; se rechaza si algún email es inválido |
| Día 31 en mes sin 31 días | Se ejecuta el último día del mes (ej: 28 para febrero) |
| Hora en zona horaria diferente | Se convierte correctamente a UTC para la ejecución |
| Más de 10 destinatarios | Permitido; si hay más de 20, se muestra warning "Considere usar una lista de distribución" |
| Reporte base eliminado | El programa se marca como ERROR; se notifica al creador |
| Email rechazado por el servidor | Se reintenta hasta 3 veces; si falla, se marca como FAILED y se notifica |
| Programación duplicada | Se permite; el sistema ejecuta ambos programas por separado |
| Cambio de horario por DST | Se ajusta automáticamente según la zona horaria configurada |
| Envío a más de 50MB | Se comprime el archivo o se genera link de descarga temporal |

---

## Criterios de Aceptación

1. El usuario puede crear programas de reportes con frecuencia diaria, semanal o mensual.
2. Cada programa permite agregar múltiples destinatarios de email.
3. El reporte se genera automáticamente en el horario configurado y se envía por email.
4. El email incluye el archivo adjunto en el formato seleccionado y un resumen en texto del contenido del reporte.
5. El programa puede activarse o pausarse en cualquier momento.
6. El sistema reintenta hasta 3 veces los envíos fallidos con backoff exponencial (1min, 5min, 15min).
7. Los programas de reportes sobreviven a reinicios del servidor (persisten en base de datos).
8. El usuario recibe una notificación si un programa falla 3 veces consecutivas.

---

## Dependencias

**RFs relacionados**:
- RF-128 — Exportación de reportes (XLSX, CSV, PDF)
- RF-130 — Log de reportes programados entregados
- RF-129 — Este requerimiento (auto-referencia para la creación del schedule desde el propio módulo)

**Tablas de base de datos afectadas**:
- `report_schedules` — Configuración de programas de reportes (nueva tabla)
- `export_jobs` — Referencias de exportación para el log
- `notifications` — Registro de notificaciones enviadas

---

## Endpoints API Involucrados

- `POST /api/v1/reports/schedules` — Crea un nuevo programa de reporte
- `GET /api/v1/reports/schedules` — Lista todos los programas del usuario/sede
- `PUT /api/v1/reports/schedules/{id}` — Actualiza un programa existente
- `PATCH /api/v1/reports/schedules/{id}/pause` — Pausa un programa
- `PATCH /api/v1/reports/schedules/{id}/resume` — Reanuda un programa pausado
- `DELETE /api/v1/reports/schedules/{id}` — Elimina un programa

---

## Métricas de Éxito

- 100% de entregas exitosas cuando el servicio de email está funcional.
- Reintentos completados en < 30 minutos desde el primer intento fallido.
- Programas activos se ejecutan dentro de una ventana de ±5 minutos de la hora configurada.

---

## Notas

- El scheduler utiliza un cronjob que consulta los programas activos cada minuto.
- Los parámetros del reporte son fijados al momento de crear el programa; no se recalculan dinámicamente.
- Para reportes que requieren contexto de fecha (ej: "últimos 7 días"), el programa cálcula el rango de fechas en cada ejecución basándose en la frecuencia.
- El email enviado incluye: asunto con nombre del reporte y período, cuerpo con resumen ejecutivo de 3-5 líneas, y archivo adjunto.
- Si el usuario quiere dejar de recibir un email programado, puede desuscribirse del enlace en el email sin cancelar todo el programa.