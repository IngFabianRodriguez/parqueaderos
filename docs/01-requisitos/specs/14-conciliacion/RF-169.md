# RF-169 — Cálculo de Efectivo Esperado vs Registrado por Turno

## Metadata

- **Módulo**: Conciliación
- **Prioridad**: Alta
- **Tipo**: Funcional
- **Servicios afectados**: [conciliacion-service, caja-service,notificacion-service]

---

## User Story

**Como** operador de caja **quiero** consultar el total de efectivo esperado versus el registrado en mi turno **para** identificar discrepancias antes del cierre.

---

## Objetivo

El sistema debe calcular y mostrar la diferencia entre el dinero físico contado en caja versus el total de transacciones registradas (cobros de pasaje, recargas, etc.) durante un turno, permitiendo al operador verificar su gestión antes de cerrar.

---

## Actores

- Operador de caja (principal)
- Administrador de sede (secundario, puede ver conciliaciones de todos los operadores)

---

## Precondiciones

- El operador tiene un turno abierto (turno con estado "abierto")
- Existen transacciones registradas en el sistema durante ese turno
- Se han registrado las aperturas de talanquera con sus montos correspondientes

---

## Postcondiciones

### En caso de éxito

- El sistema muestra el total esperado y el total registrado con la diferencia calculada
- Se genera un registro de conciliación con timestamp

### En caso de falla

- Si no hay transacciones en el turno, se muestra mensaje informativo sin cálculo
- Si el turno no existe o está cerrado, se retorna error de operación no válida

---

## Flujo Funcional

1. Operador accede al módulo de conciliación desde su dashboard
2. Sistema identifica el turno activo del operador (o permite seleccionar uno cerrado)
3. Sistema consulta transacciones del turno: cobros de pasaje, recargas, otros ingresos
4. Sistema consulta aperturas de talanquera registradas en el turno y sus montos
5. Sistema calcula total esperado = suma de todas las transacciones registradas
6. Sistema calcula total físico = suma de apertura inicial + ingresos por aperturas de talanquera
7. Sistema calcula diferencia = total físico - total esperado
8. Sistema muestra resultado con señalización visual si hay diferencia (positiva o negativa)

---

## Datos de Entrada

| Campo | Tipo | Descripción | Requerido |
|-------|------|-------------|-----------|
| operador_id | UUID | Identificador del operador | Sí |
| turno_id | UUID | Identificador del turno (opcional si hay turno activo) | No |
| sede_id | UUID | Identificador de la sede | Sí |

---

## Datos de Salida

| Campo | Tipo | Descripción |
|-------|------|-------------|
| turno_id | UUID | Identificador del turno |
| operador_id | UUID | Identificador del operador |
| total_esperado | Decimal | Suma de transacciones registradas |
| total_fisico | Decimal | Suma de montos en aperturas de talanquera |
| diferencia | Decimal | Diferencia entre físico y esperado |
| porcentaje_diferencia | Decimal | Porcentaje de la diferencia |
| estado_conciliacion | Enum | "conciliado" / "en_discrepancia" / "sin_datos" |
| fecha_ultima_conciliacion | Timestamp | Fecha y hora del cálculo |

---

## Casos de Borde y Manejo de Errores

| Escenario | Comportamiento Esperado |
|-----------|--------------------------|
| Turno sin transacciones | Mostrar total_esperado = 0, totales en cero con mensaje "Sin actividad en este turno" |
| Diferencia exactamente 0.5% | Marcar como "conciliado" (el umbral es mayor al 0.5%) |
| Diferencia mayor a 0.5% | Marcar como "en_discrepancia" y resaltar visualmente |
| Turno no existe | Retornar error HTTP 404 con mensaje "Turno no encontrado" |
| Turno ya cerrado | Permitir consulta en modo solo lectura |
| Múltiples sesiones de caja en turno | Sumar todos los montos de sesiones activas del operador |

---

## Criterios de Aceptación

1. El sistema calcula correctamente la suma de transacciones por tipo (pasaje, recarga, otros)
2. El sistema calcula correctamente la suma de montos de aperturas de talanquera
3. La diferencia se muestra con 2 decimales y el signo correcto (positivo = sobrante, negativo = faltante)
4. El porcentaje se calcula sobre el total esperado
5. El estado se marca automáticamente según el umbral del 0.5%
6. El operador puede consultar la conciliación en tiempo real durante su turno

---

## Dependencias

**RFs relacionados**:
- RF-100 — Registro de cobros de pasaje
- RF-101 — Recarga de tarjeta prepago
- RF-131 — Apertura de talanquera
- RF-170 — Marcar discrepancias mayores al 0.5%
- RF-172 — Cierre de turno

**Tablas de base de datos afectadas**:
- `transacciones` — lectura de cobros y recargas del turno
- `aperturas_talanquera` — lectura de montos de aperturas
- `turnos` — consulta de estado y datos del turno
- `conciliaciones` — escritura del registro de conciliación

---

## Endpoints API Involucrados

- `GET /api/v1/conciliacion/turno/{turno_id}` — Obtener conciliación de un turno
- `GET /api/v1/conciliacion/operador/{operador_id}/actual` — Obtener conciliación del turno activo
- `POST /api/v1/conciliacion/turno/{turno_id}/calcular` — Forzar recálculo de conciliación

---

## Métricas de Éxito

- Tiempo de respuesta de cálculo de conciliación < 3 segundos
- Porcentaje de operadores que consultan conciliación antes de cierre > 80%
- Cantidad de discrepancias detectadas vs. discrepancias no reportadas

---

## Notas

- El cálculo considera únicamente transacciones en efectivo; pagos con tarjeta se excluyen del cálculo de efectivo esperado
- El umbral del 0.5% es sobre el total de efectivo esperado
- Se debe permitir conciliaciones históricas para auditorías