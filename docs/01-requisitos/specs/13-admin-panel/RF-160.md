# RF-160 — Log de Auditoría del Admin Panel

## Metadata

- **Módulo**: 13 — Admin Panel
- **Prioridad**: Alta
- **Tipo**: Funcional
- **Servicios afectados**: audit-service, admin-service

---

## User Story

**Como** superadmin **quiero** ver el log de auditoría de todas las acciones realizadas en el sistema **para** mantener la trazabilidad y seguridad.

---

## Objetivo

El sistema mantiene un log inmutable de todas las acciones sensibles: creación/modificación/eliminación de usuarios, roles, tenants, sedes, cambios de configuración, y accesos administrativos. Cada entrada registra: quién, qué, cuándo, y valores anteriores/nuevos.

---

## Actores

- **Actor principal**: Superadmin
- **Actor secundario**: Auditor externo, Admin de tenant (solo ve los de su tenant)

---

## Flujo Funcional

### Consultar Log de Auditoría

1. Admin accede a "Auditoría" en el menú.
2. Filtra por: rango de fechas, módulo, usuario, tipo de acción.
3. Sistema muestra resultados paginados.
4. Admin puede hacer clic en una entrada para ver detalle completo.

---

## Datos de Salida — Entrada de Log

| Campo | Tipo | Descripción |
|-------|------|-------------|
| audit_id | UUID | Identificador único |
| timestamp | DateTime | Cuándo ocurrió |
| usuario_id | UUID | Quién hizo la acción |
| usuario_email | String | Email del usuario |
| modulo | String | Módulo afectado |
| accion | Enum | CREATE, UPDATE, DELETE, LOGIN, LOGOUT, ACCESS |
| recurso_tipo | String | Tipo de recurso (USER, TENANT, SITE, etc.) |
| recurso_id | UUID | ID del recurso |
| datos_anteriores | JSON | Estado antes del cambio |
| datos_nuevos | JSON | Estado después del cambio |
| ip_address | String | IP desde donde se hizo |
| user_agent | String | Navegador/cliente |

---

## Criterios de Aceptación

1. El log de auditoría es inmutable: no se puede editar ni eliminar entradas.
2. Las entradas se Retention mínimo 2 años.
3. La búsqueda/filtro retorna resultados en < 2 segundos.
4. Superadmin ve logs de todos los tenants; admin de tenant solo los suyos.
5. Se puede exportar el log filtrado a CSV.

---

## Dependencias

**RFs relacionados**:
- RF-027 — Log de auditoría
- RF-082 — Auditoría de acciones sensibles

---

## Notas

- Los logs se almacenan en base de datos separada optimizada para lectura.
- Para entornos Enterprise, se puede integrar con SIEM externo.