# RF-131 — Dashboard del Operador en App Móvil

## Metadata

- **Módulo**: 11 — App Operador
- **Prioridad**: Alta
- **Tipo**: Funcional
- **Servicios afectados**: occupancy-service, payment-service, operator-app

---

## User Story

**Como** operador de parqueadero **quiero** ver en mi app móvil un dashboard con el conteo de espacios disponibles, la ocupación en tiempo real y los ingresos del día **para** tener visibilidad inmediata del estado de la sede sin abrir múltiples pantallas.

---

## Objetivo

La app móvil del operador debe mostrar en su pantalla principal (dashboard) tres datos clave actualizados en tiempo real: (1) total de espacios disponibles vs. total de espacios, (2) porcentaje de ocupación actual de la sede, y (3) total de ingresos acumulados del día en curso. Estos datos se actualizan automáticamente cada 30 segundos o ante un cambio significativo (entrada/salida/cierre de ticket).

---

## Actores

- **Actor principal**: Operador de parqueadero (`operator_role`)
- **Actor secundario**: Supervisor de sede, Administrador de sede

---

## Precondiciones

- El operador tiene una sesión activa en la app móvil.
- El operador tiene asignada al menos una sede.
- Existe al menos un espacio configurado en la sede.

---

## Postcondiciones

### En caso de éxito

- El dashboard muestra datos actualizados de disponibilidad, ocupación e ingresos.
- Al tacto sobre cualquier métrica se navega al detalle correspondiente.

### En caso de falla

- Si no hay datos de ingresos (sede nueva), el panel muestra "Sin movimientos hoy" con valor $0.
- Si la conexión falla, se muestra el último dato disponible en caché con indicador de "sin conexión en vivo".

---

## Flujo Funcional

1. Al abrir la app, el operador ve el dashboard principal de su sede asignada.
2. El sistema consulta occupancy-service para obtener el conteo de espacios disponibles y totales.
3. El sistema calcula el porcentaje de ocupación: `(ocupados / total) * 100`.
4. El sistema consulta payment-service para obtener los ingresos del día.
5. Los datos se muestran en tres tarjetas principales: Espacios, Ocupación, Ingresos.
6. Cada tarjeta es táctil y navega al detalle:
   - Espacios → Mapa de zonas con estados.
   - Ocupación → Lista de zonas con porcentajes.
   - Ingresos → Resumen de transacciones del día.
7. El indicador de última actualización se muestra en la parte inferior del dashboard.

---

## Datos de Salida

| Campo | Tipo | Descripción |
|-------|------|-------------|
| espacios_totales | Integer | Total de espacios configurados en la sede |
| espacios_disponibles | Integer | Espacios actualmente sin vehículo |
| espacios_ocupados | Integer | Espacios actualmente con vehículo |
| porcentaje_ocupacion | Float | Porcentaje de ocupación (0.0 – 100.0) |
| ingresos_dia | Decimal | Total de ingresos del día en moneda local |
| cantidad_vehiculos_dia | Integer | Total de vehículos que ingresaron hoy |
| tickets_abiertos | Integer | Cantidad de tickets actualmente abiertos |
| ultimo_update | DateTime | Timestamp de la última actualización de datos |

---

## Comportamiento de Tarjetas del Dashboard

| Tarjeta | Valor Principal | Valor Secundario | Color de Indicador |
|---------|-----------------|------------------|--------------------|
| Espacios | `X / Y disponibles` | `Z ocupados` | Verde si >50%, Amarillo si 20-50%, Rojo si <20% |
| Ocupación | `XX%` | `X veh.` | Verde si <70%, Amarillo si 70-90%, Rojo si >90% |
| Ingresos | `$MMMMM` | `X tickets` | Sin indicador de color |

---

## Casos de Borde y Manejo de Errores

| Escenario | Comportamiento Esperado |
|-----------|--------------------------|
| Sin movimientos hoy | Panel de ingresos muestra "$0 — Sin movimientos hoy" |
| Error de conectividad | Se muestra última versión en caché + indicador fuera de línea |
| 0 espacios disponibles | Tarjeta Espacios en rojo con mensaje "Capacidad completa" |
| Ocupación 100% | Tarjeta Ocupación en rojo con mensaje "Sede llena" |
| Más de 999.999 en ingresos | Se muestra en formato abreviado (ej: "$1.2M") |
| Sesión expirada durante uso | Se redirige a login y luego al dashboard |

---

## Criterios de Aceptación

1. Al abrir la app, el dashboard se carga en menos de 3 segundos.
2. El operador ve en todo momento el conteo de espacios disponibles actualizado.
3. El porcentaje de ocupación se calcula correctamente.
4. Los ingresos del día se muestran en la moneda configurada de la sede.
5. Los datos se actualizan automáticamente cada 30 segundos sin intervención del operador.
6. Si hay menos del 20% de espacios disponibles, el indicador cambia a rojo.
7. Cada tarjeta navega al detalle correspondiente al tocarla.
8. El dashboard funciona en modo offline mostrando datos en caché.

---

## Dependencias

**RFs relacionados**:
- RF-009 — Gestión de espacios y zonas
- RF-042 — Procesamiento de pagos
- RF-063 — Monitoreo de ocupación en tiempo real

**Servicios consultadas**:
- `occupancy-service` — datos de espacios
- `payment-service` — datos de ingresos

**Tablas de base de datos afectadas**:
- `parking_spaces` — conteo de espacios
- `tickets` — tickets abiertos y count de vehículos
- `payments` — suma de ingresos del día

---

## Endpoints API Involucrados

- `GET /api/v1/operator/dashboard` — Retorna todos los datos del dashboard para la sede del operador
- `GET /api/v1/operator/dashboard/spaces` — Detalle de espacios por zona
- `GET /api/v1/operator/dashboard/revenue?date={date}` — Ingresos del día

---

## Métricas de Éxito

- Dashboard carga en < 3 segundos en red 4G.
- Los datos de ocupación no se desfasan más de 30 segundos respecto a la realidad.
- El indicador visual de disponibilidad (color) refleja correctamente el estado.
- 0 errores de UX al cargar dashboard en condiciones normales.

---

## Notas

- El dashboard es la pantalla principal al abrir la app; no requiere navegación adicional.
- Se debe implementar un mecanismo de caché local para funcionar sin conexión temporal.
- Los colores de indicador deben ser distinguibles para daltonismo (no solo rojo/verde, usar iconos también).