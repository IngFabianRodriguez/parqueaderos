# RF-066 — Tracking de Eventos Significativos de Suscripción

## Metadata

- **Módulo**: SaaS Multi-Tenant — Métricas SaaS
- **Prioridad**: Alta
- **Tipo**: Funcional
- **Servicios afectados**: analytics-service, billing-service, tenant-service

---

## User Story

**Como** sistema de ParkCore **quiero** trackear eventos significativos de suscripción **para** calcular métricas SaaS como MRR, Churn Rate y NRR, y permitir análisis de comportamiento de los tenants a lo largo del tiempo.

---

## Objetivo

El sistema debe registrar en la tabla `subscription_events` cada evento relevante del ciclo de vida de suscripción: `trial_started`, `converted`, `upgraded`, `downgraded`, `churned`, `payment_failed`. Estos eventos alimentan las métricas de negocio y permiten auditoria histórica del comportamiento de cada tenant.

---

## Actores

- **Actor principal**: Sistema (registra eventos automáticamente)
- **Actor secundario**: Superadmin (consulta eventos para dashboards)

---

## Precondiciones

- El tenant existe en la base de datos con un `tenant_id` válido.
- El flujo de suscripción ha sido iniciado (trial o paid).

---

## Postcondiciones

### En caso de éxito

- El evento se inserta en `subscription_events` con timestamp y metadata asociada.
- Si el evento dispara un cambio de estado en el tenant (ej: `churned`), el tenant se actualiza correspondientemente.
- Métricas agregadas (MRR, Churn Rate) se recalculan en background job.

### En caso de falla

- Si la inserción falla, el error se loguea pero no afecta el flujo principal del tenant.
- El evento queda pendiente para reintento en el siguiente job de sincronización.

---

## Flujo Funcional

1. Un evento ocurre en el sistema de billing o gestión de suscripciones (webhook de Stripe, acción del usuario, cambio de plan).
2. El servicio que procesa la acción (webhook-service, subscription-service) detecta que es un evento trackeable.
3. Se construye el payload del evento:

   - `event_type`: uno de los tipos definidos.
   - `tenant_id`: ID del tenant afectado.
   - `plan_id`: plan vigente después del evento (si aplica).
   - `previous_plan_id`: plan anterior (para upgrades/downgrades).
   - `amount`: monto facturado o proyectado (para eventos de revenue).
   - `metadata`: JSON con datos adicionales específicos del evento.
   - `occurred_at`: timestamp UTC del evento.

4. Se inserta en `subscription_events`.
5. Se actualiza el estado del tenant según el tipo de evento.
6. Se encola un job para recalcular métricas agregadas.

### Tipos de eventos y su lógica

| event_type | Cuándo se dispara | Datos adicionales |
|------------|------------------|-------------------|
| `trial_started` | Tenant inicia periodo de prueba | `trial_end_date`, `plan_id` |
| `converted` | Trial se convierte a subscription paga | `subscription_id`, `plan_id` |
| `upgraded` | Plan cambia a uno de mayor precio | `previous_plan_id`, `new_plan_id`, `amount_delta` |
| `downgraded` | Plan cambia a uno de menor precio | `previous_plan_id`, `new_plan_id`, `amount_delta` |
| `churned` | Suscripción se cancela | `churn_reason`, `cancellation_date` |
| `payment_failed` | Pago falla (intento 1, 2 o 3) | `attempt_number`, `invoice_id`, `failure_code` |
| `payment_failed_final` | Tercer intento falla y se dispara suspensión | `invoice_id`, `tenant_status` |

---

## Datos de Entrada

| Campo | Tipo | Descripción | Requerido |
|-------|------|-------------|-----------|
| event_type | string | Tipo de evento (`trial_started`, `converted`, `upgraded`, `downgraded`, `churned`, `payment_failed`) | Sí |
| tenant_id | uuid | ID del tenant | Sí |
| plan_id | uuid | Plan asociado al evento | Sí |
| previous_plan_id | uuid | Plan anterior (para upgrade/downgrade) | No |
| subscription_id | string | ID de la suscripción en Stripe | No |
| amount | decimal | Monto en la moneda del tenant | No |
| metadata | jsonb | Datos adicionales del evento | No |
| occurred_at | timestamp | Fecha y hora del evento | Sí |

---

## Datos de Salida

| Campo | Tipo | Descripción |
|-------|------|-------------|
| id | uuid | ID único del evento registrado |
| event_type | string | Tipo de evento |
| tenant_id | uuid | ID del tenant |
| recorded_at | timestamp | Timestamp de registro en el sistema |

---

## Casos de Borde y Manejo de Errores

| Escenario | Comportamiento Esperado |
|-----------|--------------------------|
| Evento duplicado ( mismo `event_type` + `tenant_id` + `occurred_at`) | Ignorar la inserción; retornar el registro existente (idempotencia por clave natural). |
| `tenant_id` no existe | Loguear warning; no insertar evento; no afectar flujo principal. |
| Campos faltantes en `metadata` | Insertar con `metadata = {}`; no fallar. |
| Servicio de base de datos no disponible | Encolar el evento en una cola `pending_events` para procesamiento posterior. |
| Retry de evento ya procesado | Verificar por `event_id` de Stripe si existe; si ya fue procesado, skip. |

---

## Criterios de Aceptación

1. Todos los 7 tipos de eventos (`trial_started`, `converted`, `upgraded`, `downgraded`, `churned`, `payment_failed`, `payment_failed_final`) se registran correctamente en `subscription_events`.
2. Cada registro incluye `tenant_id`, `event_type`, `occurred_at` y `metadata` completa.
3. No hay eventos duplicados para la misma combinación de tenant + evento + timestamp.
4. El registro de eventos no bloquea el flujo principal de suscripción/cancelación.
5. Los eventos están disponibles para consumo por el servicio de métricas (RF-063, RF-064, RF-065).
6. Latencia de inserción < 100ms para el 95% de los casos.

---

## Dependencias

**RFs relacionados**:
- RF-032 — Conversión trial a subscription (dispara `converted`)
- RF-035 — Suspensión por pago fallido (dispara `payment_failed_final`)
- RF-036 — Churn (dispara `churned`)
- RF-037 — Selección de plan (dispara `trial_started` o `upgraded`)
- RF-045 — Webhooks de Stripe (fuente de `payment_failed`, `converted`, `churned`)
- RF-063 — MRR (consume eventos para cálculo)
- RF-064 — Churn Rate (consume eventos)
- RF-065 — NRR (consume eventos)

**Tablas de base de datos afectadas**:
- `subscription_events` — tabla principal de eventos
- `tenants` — actualización de `status` según evento
- `subscriptions` — actualización de `plan_id` según upgrade/downgrade

---

## Endpoints API Involucrados

- No expone endpoints REST nuevos; el tracking ocurre internamente via eventos del sistema.
- `GET /api/v1/admin/subscription-events` — Consulta de eventos para superadmin (listado paginado, filtrable por tenant, tipo, rango de fechas).

---

## Métricas de Éxito

- Tasa de eventos registrados sin error: 99.9%
- Cero eventos duplicados por clave natural
- Tiempo de inserción promedio < 50ms
- Cobertura: 100% de los eventos del lifecycle de suscripción se trackean

---

## Notas

- Los eventos son inmutables una vez insertados; no se actualizan ni eliminan.
- Se usa una clave única compuesta (`tenant_id`, `event_type`, `occurred_at`) para garantizar idempotencia.
- Para debugging, el campo `metadata` puede contener el `event_id` de Stripe cuando el evento proviene de un webhook.
- El servicio de analytics consume estos eventos en tiempo real via message queue para actualizar dashboards.